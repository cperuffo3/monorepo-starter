/**
 * Re-exports of generated Prisma model types.
 * The rest of the app must import entity types from here (or the database
 * barrel) — importing from prisma/generated outside src/database is
 * lint-enforced as an error.
 */
export type { User } from '../../prisma/generated/prisma/client.js';
